your goal is to build all images and run them into Docker desktop

Do the following

1 run 'docker compose --env-file docker-local.env -f docker-compose.yml build' to build all docker project images
2 run  'docker compose --env-file docker-local.env -f docker-compose.yml up -d' to run refresh the images docker containers are running
